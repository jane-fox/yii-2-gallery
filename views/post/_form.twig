<?php

use yii\helpers\Html;
use yii\widgets\ActiveForm;

/* @var $this yii\web\View */
/* @var $model app\models\Post */
/* @var $form yii\widgets\ActiveForm */
?>

<div class="post-form">

	<?php $form = ActiveForm::begin([

		"options" => ['enctype' => 'multipart/form-data']
		//"class"=>"dropzone",
	]); ?>

	<?= $form->field($model, 'file')->hiddenInput()->label(false); ?>


	<?= $this->render('_tag_picker') ?>

	<div class="row margin-bottom">
		<div class="col-sm-8 col-sm-offset-2 ">
			<?= $form->field($model, 'text')->textarea()->label("Description &nbsp; <small> (optional)</small>") ?>
		</div>
	</div>

	<div class="col-sm-8 col-sm-offset-2">
		<?= Html::submitButton($model->isNewRecord ? 'Create' : 'Update', ['class' => 'btn btn-lg btn-primary']) ?>
	</div>

	<div class="form-group">
	</div>

	<?php ActiveForm::end(); ?>

</div>



